import { Stack } from '@mui/material';

// todo it could be server side rendered as it should be static, if needed (figma prototype doesn't have it)

export const Footer = () => {
  return <Stack></Stack>;
};
